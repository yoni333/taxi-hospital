/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShiftPipe } from './shift.pipe';

describe('Pipe: Shifte', () => {
  it('create an instance', () => {
    let pipe = new ShiftPipe();
    expect(pipe).toBeTruthy();
  });
});
